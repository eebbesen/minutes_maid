<%= form_with(model: note, local: true) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="field hidden">
    <%= form.label :item_id %>
    <%= form.text_field :item_id, value: (@item ? @item.id : note.item_id) %>
  </div>

  <div class="actions">
    <%= form.submit nil, { class: 'button hollow header-button' } %>
  </div>
<% end %>
